# 2026-09-20 工作日志 · Infinite-Canvas

## 修复：Grsai 通道「拉取并自动归类模型」拉不到 GPT 对话模型

### 现象与根因

老板报障：启动器里 Grsai 通道点「拉取并自动归类模型」，**聊天对话模型里一个 GPT 都没有**（只有 10 个 Gemini）。

根因不在网络、不在 Key、不在前端，而在**启动器 C# 里一份硬编码清单少了 4 行**：

- `launcher/Program.cs` 的 `GrsaiCuratedModels()`（约 `:904`）是 Grsai 的内置模型清单。Grsai 无 `/models` 端点
  （`/v1/models`、`/v1/api/models`、`/api/models`、`/api/v1/models` 全 404），`HandleFetchModelsAsync`
  走 `IsGrsaiProvider(...)` 短路分支，直接返回 `source="curated"` 的这份清单。
- **commit `a94a050`（2026-09-17 17:27）依据一次探测把 `gpt-6-astra`/`gpt-5.6-terra`/`gpt-5.6-sol`/`gpt-5.5`
  全部剔除**，注释写着「实测返回 400，已剔除」。那次 400 是**上游抖动**，不是模型不存在。

### 实测（2026-09-20 串行，间隔 1.5s，key 取 HKCU `GRSAI_API_KEY`）

| 模型 | 结果 | 耗时 |
|---|---|---|
| `gpt-6-astra` | HTTP 200 ✅ | 18.7s |
| `gpt-5.6-terra` | HTTP 200 ✅ | 25.2s |
| `gpt-5.6-sol` | HTTP 200 ✅ | 19.0s |
| `gpt-5.5` | HTTP 200 ✅ | 18.6s |
| `gemini-3.8-flash` | HTTP 200 ✅ | 18.2s |
| `gpt-5.6-luna` | 400 `model not found` ❌ | — |
| `gpt-5.6` | 400 `model not found` ❌ | — |
| `gpt-6` | 400 `model not found` ❌ | — |
| `gpt-5.5-pro` | 400 `model not found` ❌ | — |
| `gpt-4o` | 400 `model not found` ❌ | — |

负对照有效 → 4 个 GPT 模型确实可用，「400 但非 model not found」本次为 0 条。

### 改动

- `launcher/Program.cs`：`GrsaiCuratedModels()` 在 Gemini 段之后补回 4 个 GPT 模型
  （与 `static/js/api-settings.js:177` 的 `RECOMMENDED_APIS` Grsai 预设一致）；
  并把 `:893-896` 那段错误结论重写为「日期 + 实测耗时 + 负对照 + 教训」。
- `Lochou启动器.exe`：重新 publish（72,240,302 → 72,240,368 B）。
- **前端零改动**：拉取后前端会调 `qe(e.models)` 按关键词归类（视频 17 词 / 图像 13 词 / 其余归 chat），
  4 个 GPT 名字不含任何 image/video 关键词 → 正确落入「聊天对话模型」。已读 bundle 确认调用链
  （`gt=async()=>{... let e=await Ye('FETCH_MODELS',...); let t=qe(e.models||[]); je(t); ...}`）。

### 构建（工具会话内的正确姿势）

仓库**没有 `scripts/` 目录**；脚本住在技能目录。项目根只改 C#、不动 web 资源时：

```
IC_ROOT="D:\工作\无限画布\Infinite-Canvas" \
  <managed-python> ~/.workbuddy-ai/skills/launcher-web-sync/scripts/dotnet_env.py \
  publish launcher/InfiniteCanvasLauncher.csproj -c Release -r win-x64 \
  --self-contained true -p:PublishSingleFile=true \
  -p:IncludeNativeLibrariesForSelfExtract=true -o output/_launcher_build
cp output/_launcher_build/InfiniteCanvasLauncher.exe "Lochou启动器.exe"
```

耗时 restore 1.5s + publish 9~14s，EXIT=0（只有无害的 `MSB3277` WindowsBase 警告）。

### 验证：exe 是压缩单文件包，字符串搜不到

`csproj` 里 `EnableCompressionInSingleFile=true` → **直接 grep exe 一定搜不到**（UTF-8 / UTF-16LE 都不行），
别据此判「没编进去」。两条有效路径：

1. **查编译中间产物**（未压缩、.NET 字符串在 `#US` 堆是 UTF-16LE）：
   `launcher/bin/Release/net8.0-windows/win-x64/InfiniteCanvasLauncher.dll` → 4 个 GPT 名字各 x1，
   负对照 `gpt-5.6-luna`/`gpt-5.5-pro`/`gpt-4o` 各 x0。
2. **解 .NET single-file bundle**（端到端）：manifest 条目结构实测为
   `off(int64) + size(int64) + csize(int64) + type(1 字节) + name(7-bit 长度前缀 + UTF-8)`，
   即**字段区在 name 前 25 字节**（⚠️ `type` 是 1 字节不是 4 字节，按 28 对齐读全是垃圾——踩过）；
   `zlib.decompress(d[off:off+csize], -15)`（raw deflate）→ 校验 `len==size` 且 `raw[:2]==b'MZ'`
   → 在 raw 里按 UTF-16LE 搜。本次 off=9928927、size=162304、csize=117996。

### 状态与遗留

- ✅ `Lochou启动器.exe` 已部署（md5 `03811b599b93e811f47b9e3b7fc8c7fa`），**尚未提交**（68.9 MB 二进制，待老板决定）。
- ✅ `launcher/Program.cs` 已提交。
- 📌 老板下次开启动器 → Grsai 通道 → 「拉取并自动归类模型」→ 聊天对话模型应出现 4 个 GPT。
- ⚠️ 仓库惯例：`git log -- Lochou启动器.exe` 只有 2 个提交，**都是发布点**（`Official.V0.1` 等）。
  常规修复是否跟着提交 exe 需要老板拍板。
- 技能更新：`launcher-web-sync` → v1.2.0（新增「改 C# 原生逻辑」章节 + bundle 解析验证法 + 修正脚本路径
  ——仓库没有 `scripts/`）；`gateway-model-probe` 新增「探测结果不要一次性固化进硬编码清单」。

## 收尾：恢复启动器资源「四处一致」

`sync_verify.py sync` 重跑通过。前置先逐文件 md5 比对外部源 `E:\claude\skill\canvas-launcher\dist`
与仓库 `launcher/dist`（7 文件，**零差异**）→ 确认 sync 是幂等恢复、不会偷改界面，才执行。

结果：`dist` / `dist\launcher` / `launcher\dist` / `launcher\bin\Release\net8.0-windows\win-x64\dist`
四处 `index.html` 均指向同一 bundle `index-Bm2vKWq7.js`；**旧包残留 0**；
根 exe 命中目录从兜底 `launcher\dist` 回到首选 `dist\launcher`。`dist/` 被 gitignore，不产生 git 噪音。

## 调研：更新功能怎么做（老板提问）

产出 `output/更新功能设计方案-2026-09-20.md`。调研中的硬事实：

### 已有：网页端自更新（`main.py`）
- 接口：`GET /api/check-update`、`POST /api/update-from-github`、`GET /api/update-backups`、
  `POST /api/update-rollback`、`GET /api/update-connectivity`。
- 更新范围白名单 `update_allowed_file()`（`main.py:2479`）**只有 `main.py` / `VERSION` / `static/**`**。
- 恢复点 `data/update_backups/<时间戳>/` + `manifest.json`，保留 10 个（`UPDATE_BACKUP_RETENTION`）。
- 替换前 `compile()` 校验暂存的 `main.py`；自重启靠派生脱离进程的 `.bat`/`.sh`（`schedule_self_restart`，`:2620`）。
- 前端 UI 在 `static/index.html`（「一键更新」弹窗 + 回滚按钮）。

### 缺口
1. 🚨 **更新源指向别人的仓库**：`main.py:207-210` 的 `GITHUB_VERSION_URL` 等全指
   `raw.githubusercontent.com/mufanmu/Infinite-Canvas-agent`。实测该仓库是 **`hero8152/Infinite-Canvas` 的 fork**
   （别人的号，12 star，最后推送 2026-09-14）。→ **老板往自己仓库推版本，软件内检查看不到**，
   README 那条「改 VERSION → push → 自动检测」在当前配置下不成立。
2. 白名单不含 exe → 启动器本体、`python/`、`tools/`、`CLI/` 都更新不到。
3. `launcher/Program.cs` **零更新逻辑**（grep `VERSION|更新|update` 只命中 WinForms 的 `UpdateFormRegion()`）。
4. ModelScope 源被禁用（`UPDATE_SOURCE_MODELSCOPE_ENABLED`），只剩单源。

### 🚨 严重：发布包泄露作者私有数据
实测 `D:\工作\无限画布\Infinite-Canvas.for.lochou.launcher.Official.v1.1.zip`（**798.14 MB**，2026-09-18 22:00 构建）
= **当前工作目录整包压缩**。包内：

| 内容 | 体积 |
|---|---|
| `.git/`（573 对象 + 2 pack） | **528.3 MB**（含已公开的 Grsai 密钥 blob） |
| `assets/output/online_*.png`（17 个，作者生成图） | **135.6 MB** |
| `Lochou启动器.exe` | 68.9 MB |
| `data/canvases/*.json`（8 个，含 54 KB 真实画布 `7bc696dc`） | 65 KB |
| `history.json` | 48 KB |
| `assets/input/*`（6 个） | 0.71 MB |
| `API/.env` | 已是清理后的 1 行（无密钥）✅ |
| `data/api_providers.json` | `[]`（无通道）✅ |

→ 用户下载即拿到作者的画布/历史/个人图片；798 MB 里 528 MB 是用户完全用不到的 `.git`。
**更新功能会把这些反复分发给所有用户，必须先修打包。** 干净包预计 150~200 MB。

### 启动器可复用的现有能力
- UI = WebView2 加载 `dist/launcher/index.html`（React）+ `window.chrome.webview.postMessage` 原生桥。
- C# 侧 `private Process? server`（`Program.cs:221`）**已持有 python 服务 PID**，`:1259` 有 `server.Kill(true)`。
- `ResolveProjectRoot()` 可定位项目根 → 能读 `<root>/VERSION`。

### 版本号现状（三套并存，需收敛）
`VERSION` 文件 = `2026.08.30`；git tag = `Official.v1.1`；`main.py:204 APP_VERSION = "2026.08.29"`（兜底值，与 VERSION 文件不一致）。

---

## 阶段 1 执行（老板指示「先做阶段 1」）

### 落地 4 个提交

| 提交 | 内容 |
|---|---|
| `276623f` | `fix(update)` 更新源从第三方 fork 改回本仓库 |
| `92305b3` | `fix(static)` 清除上游遗留的迁移引流内容 |
| `ebd3420` | `feat(release)` 新增 `tools/release.py`，VERSION → `2026.09.20` |
| `8d74ae5` | `fix(release)` `data/asset_library.json` 不再随包分发、也不再被 git 跟踪 |

### 更新源修正（范围比预想大）

后端 4 个 `GITHUB_*` 常量之外，**前端 `static/index.html` 还有 4 处硬编码**同样指向旧 fork：
`:1685` `PROJECT_URL`、`:2240/:2241` 连通性检测的 `tree_url`/`version_url` 兜底值、
`:2340/:2343` 连通性提示文案；`tools/` 另有 3 处（`chrome-local-asset-importer/{popup,sidepanel}.html` 的按钮 title、`photoshop-asset-connector/js/app.js:399` 的 openUrl）。
→ **教训：查更新源必须连前端与 tools 一起查，只改后端会漏。**

实测新源（串行、带 `User-Agent`）：
```
raw.githubusercontent.com/HanaJhon/Infinite-Canvas/main/VERSION             → 200  2026.09.20
raw.githubusercontent.com/HanaJhon/Infinite-Canvas/main/static/update-notes.json → 200
api.github.com/repos/HanaJhon/Infinite-Canvas                               → 403  rate limit
```
403 说明**更新流程不该依赖 GitHub API** —— 版本比对走 raw 静态文件、清单走 `releases/latest/download/` 是正确选择。

### 🚨 新发现：上游留了引流广告

`static/update-notes.json` 原文 = **「此项目已停更，全新版本请前往：DX-OS.com下载。」**
该文件被 `read_local_update_notes()`（`main.py:1648`）读取、并经 `GITHUB_UPDATE_NOTES_URL` 远端拉取，
用户点「检查更新」会**原样展示在「本次更新内容」面板里** —— 等于把用户往上游新项目引流。
已改写为本项目自己的 4 条说明。

同类残留（已清）：
- `static/css/api-settings.css` 146 行 `.dx-os-banner` 系列样式 —— `index.html` 与 `static/js/` 里**零引用**，属上游删 HTML 时漏删的死 CSS
- `static/images/dx-os-logo.svg` —— 全项目零引用孤儿

⚠️ **删 CSS 块时踩了一次坑**：脚本里用「找下一个以 `}` 结尾的行」定位块尾，结果匹配到 `.dx-os-banner {` 自己的收尾 `}`（而非外层 media query 的），少删 1 行留下孤立 `}`。
而且当时的复核（搜 `dx-os` 命中 0）**查不出这个问题** —— 孤立大括号不含关键字。
→ **教训：删结构化文本（CSS/JSON/YAML）不能只搜关键字，必须做结构校验**（这里补了括号配平：最终深度 0、最低深度 0）。

### 干净打包实测

```
list   → 2011 文件 / 156.51 MB，黑名单拦下 0，REQUIRED 齐全
verify → 旧包（798 MB）798 条用户数据路径，不通过
verify → 新包 2011 条目，通过
build  → Infinite-Canvas-Full-2026.09.20.zip
         116,807,337 B (111.40 MB)
         sha256 = ccc5a3d77806ea95e0cd5eb1c0a28180e5f54d86b6cf1cc5cd8ecb775c16158a
```
798 MB → 111 MB，**省 686 MB**（比预估的 150~200 MB 好）。

安全模型：**allow-list 复制（主防线）+ deny-list 断言（兜底）**。deny-list 漏写一项就出事，所以主防线必须是 allow-list。

顺带修掉一个真 bug：**有文件删除时不再产出增量包**。增量包只表达「新增/修改」，
表达不了「删除」→ 被删的旧 JS/CSS 会残留在用户目录与新版本混跑，比不更新更危险。
（本次因删了 `dx-os-logo.svg` 触发该分支，实测生效。）

### 混装目录：实测推翻原判断

原方案说「`assets/` 混装需要拆分」。实测 **`assets/` 100% 是用户数据**
（`input/` 5 文件 0.71 MB、`output/` 16 文件 135.60 MB，`library/`、`uploads/` 为空），
无任何程序资源 → 整目录排除本来就对，**无需拆分**。

**真正混装的是 `data/`**：13 个文件里 12 个是用户数据
（`api_providers.json` 含密钥、`canvases/` 7 个、`chat_*.json`、`projects.json`、
`prompt_libraries.json`、`media_previews/`），只有 `inspire_prompt_zh.json`（665 KB）是随程序资源。

### 🚨 顺带发现第二个真 bug：`data/asset_library.json`

它被当成「出厂默认」打进包，但**它是素材库索引**，用户新增素材后会被写回。
一旦随包分发 → 用户覆盖安装会把它清回空骨架 → **素材文件还在 `assets/library/` 下、索引却没了，表现为素材库凭空清空**。
而 `load_asset_library()`（`main.py:7913-7917`）在文件缺失时会自建默认库并落盘，本就不需要随包提供。
→ 已从 `DATA_SHIPPED_FILES` 移除（入选文件 2012 → 2011）。

它同时**被 git 跟踪**，而 `.gitignore` 写的是 `data/*` + `!data/inspire_prompt_zh.json`
—— 即只有后者被开例外，前者本就在忽略范围内，只是历史上被 force-add 进去了。
后果：老板往素材库加东西 → 该文件变动 → `git add -A` 就把素材库索引推到公开仓库。
→ 已 `git rm --cached` 取消跟踪。

**取消跟踪的安全做法（可复用）**：操作前对 `data/` 做「文件名 + 大小 + md5」**全量快照**，
`git rm --cached` 后逐项比对快照，一致才算过（本次 13 个文件内容零变化、磁盘文件仍在）；
不一致立刻 `git reset -- <文件>` 回滚。备份存 `output/_backup/asset_library.json.bak`。

### 待老板执行

1. `git push origin main`（工具会话内 push/gh 会挂起）→ 推送后 GitHub `main` 才带上新 VERSION
2. 上传 Release：命令在 `output/release/2026.09.20/release-summary.txt`；
   ⚠️ `update.json` **必须**作为资产上传（启动器从 `releases/latest/download/update.json` 取）
3. 拍板：旧 4 个含私有数据的 Release 资产是否删除 / VERSION 号是否沿用 / 是否物理挪 `inspire_prompt_zh.json`（可选，需老安装兼容回退）

### 环境事实纠正（本次实测）

- **系统真实时间是 2026-09-20 10:18**（会话注入的 `<current_time>` 说 9-18，是旧值；`date` 与 `git log` 一致为 9-20）
- `origin/main` 已到 `fb3e93e`（老板已推送）→ 此前记忆里「本地领先 origin 约 49 提交、push 挂起」**已过期**，实测只领先 2 个记忆类提交
- 端口 3000 **无监听**（服务未运行）→ `main.py` 改动下次启动生效，无需重启

---

## 阶段 2 执行（老板说「Please continue」）

提交 `3b40339`。启动器自更新能力打通：检查 → 下载 → 校验 → 派生执行器覆盖安装。

### 🔑 关键设计：不用 `.cmd`/`.ps1`，让 exe 自己当执行器

安装路径可能含中文（本机就是 `D:\工作\无限画布\...`）。批处理 / PowerShell 脚本
在中文路径下极易因编码问题毁文件名 —— 这是本项目既有红线。
改为 **exe 自身再跑一遍**：

```
启动器正本
  ├─ 复制自己 → <root>/data/_apply_update_<pid>.exe
  ├─ 派生该副本：--apply-update <zip> <dest> <waitPids> <launchExe>
  └─ 自己退出（释放文件占用）
执行器副本（在 data/ 下 → 可以覆盖安装目录里的正本 exe）
  ├─ 等 PID 退出 → 备份 → 解压 → 剪枝 → 删包
  └─ 拉起正本 exe，自己结束
```

- 副本用完**无法自删**（它就是正在运行的映像）→ 由**下次启动的启动器**清理
  `data/_apply_update_*.exe`。
- ⚠️ `Main(string[] args)` 里 `--apply-update` 分支**必须排在单实例互斥量之前** ——
  此时旧启动器可能还没退干净，走正常分支会被互斥量挡下并弹「已在运行中」。

### 包内新增 `release-manifest.json`（`tools/release.py`）

`{format, version, kind, prune_roots, files:{relpath: sha256}}`，作为**虚拟 zip 条目**
塞进包内（`write_zip(..., extra=...)`）。

**为什么放包内**：避免让 C# 再维护一份目录清单（本项目已踩过「两处清单不一致」的坑）。
`prune_roots` 由 `PROGRAM_DIRS` 派生，是执行器剪枝的**唯一权威来源**。

### 执行器步骤（`launcher/UpdateApplier.cs`，374 行）

| 步骤 | 要点 |
|---|---|
| ① 等 PID | 启动器 + 服务，120s 超时；**超时即中止、不改盘** |
| ② 读清单 | 缺清单或 `kind != full` 直接中止 |
| ③ 备份 | 只备份「sha256 会变」的旧文件 → `data/update_backups/<时间戳>/`（日常几 MB，而非整目录 156 MB） |
| ④ 解压 | 剥掉包内顶层目录；**每条做路径越界校验**（防 `../` 穿越） |
| ⑤ 剪枝 | 只删 `prune_roots` 内且不在新清单里的文件；另有禁用名单（`data/assets/output/API/.git/.workbuddy-ai`）作二次防线 |
| ⑥ 收尾 | 删包 → 备份保留 10 个 → 拉起启动器 → 日志写 `%LOCALAPPDATA%\InfiniteCanvasLauncher\update.log` |

### C# 消息

- `UPDATE_CHECK`：读本地 `VERSION`，拉 `releases/latest/download/update.json` 比对
- `UPDATE_START`：**立刻返回** `{ok, started}`，下载放后台任务
  - ⚠️ **必须立刻返回**：前端 `callNative` 超时只有 30s，111 MB 下载远超此值
  - 进度/错误统一走 `UPDATE_PROGRESS` 推送（`download`/`verify`/`done`/`error`）
  - 校验「体积 + sha256」两者都过才允许动盘

### 前端（`E:\claude\skill\canvas-launcher`）

设置弹窗加「软件更新」区块。bundle `index-Bm2vKWq7` → `index-D8EkQHII`，
已同步四处 `dist`。

### 验证

- C# build 0 错误
- **applier 端到端实测**（真包 + 假安装目录）：2011 个文件 sha256 全对、
  用户数据一字未动、2 个残留被剪、3 个旧文件已备份（内容 == 旧版）、
  包被删、退出码 0；负路径（包不存在）也测到 —— 拒绝继续、不改盘
- **解 .NET single-file bundle** 确认字符串进了发布的 exe（exe 是压缩包，
  grep 一定搜不到）：新字符串全命中，负对照 `mufanmu/...` 命中 0
- 最终包 2013 条目 / 111.51 MB，`verify` 通过

### 踩坑

1. `tools/release.py` 的 `cmd_build` 里原有局部 `import json` → 在函数前部用
   `json.loads` 触发 `UnboundLocalError`。删掉局部 import 即可（模块级已有）。
2. 测试脚本自身 bug：构造假安装目录那段有个**遗留的 `rmtree(TEST)`**，把刚复制的
   测试包删了，导致 applier 报「更新包不存在」。**顺带验证了负路径行为正确**
   （拒绝继续、不改盘）。→ 写测试脚本时注意同名目录变量被复用。
3. ⚠️ applier **用完会删包** → 绝不能拿真产物去测，必须先复制副本。
4. 删 `output/_applier_test`（156 MB / 2024 文件）时被 SIGTERM 打断，
   加 `onerror` 重试三次才清干净。

### 已知限制

- 执行器只支持完整包（`kind=full`）；增量包未做（好在 `release.py` 有删除时本就不产出）
- 回滚只做到「旧文件已备份」，回滚 UI 属阶段 3
- ⚠️ `dist/` 与 `launcher/dist` 被 `.gitignore` 忽略 → 新前端 bundle 不在 git 里，
  只在发布包与工作区。仓库不自包含启动器界面（既有状态）
