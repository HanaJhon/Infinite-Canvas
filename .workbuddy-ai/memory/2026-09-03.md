# 2026-09-03

## 安全扫描：API 密钥暴露检测
- 检测了 Infinite-Canvas-agent 项目的密钥暴露情况。
- **发现**：`API/.env` 含 3 个真实 `sk-` API 密钥（FHL / CODER / CUSTOM），明文存放于工作目录。
- 已确认：非 git 仓库（无 .git）；`.gitignore` 已忽略 `API/.env`；`dist/`/`packages/` 内无 .env 副本；`workflows/*.config.json`、`data/api_providers.json`、`static/runninghub/api_providers.json` 均不含鉴权密钥（仅 base_url/appId/工作流定义）。
- 全量扫描（排除 python venv、WebView2 缓存、launcher 构建产物）仅 `API/.env` 命中。
- 建议：立即在提供方轮换这 3 个 key；勿将项目目录整体打包/同步分享；保持 `.env` 不入库。
