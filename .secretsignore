# .secretsignore —— tools/scan_secrets.py 的误报豁免清单
#
# 每行一个「路径子串」，命中的文件不参与密钥扫描。空行与 # 开头的行忽略。
# 只在确认是误报时才加，**不要**用它来放过真密钥。
#
# 例：
#   static/vendor/js/tailwindcss-cdn.js     ← 第三方库里的示例串
#   docs/api-examples.md
#
# 紧急绕过提交钩子（确认误报时）：git commit --no-verify
#
# ⚠️ 以下是已确认的误报项。
#
# fontTools 是随包的第三方办公库（fpdf2 的依赖，用于 TTF 子集化）。
# 这一行里的 CURVE_TYPE_LIB_KEY 是 cu2qu 的 OpenType 特性标识符
# （"com.github.googlei18n.cu2qu.curve_type"），被「长随机串」尺子误判成密钥。
# 只豁免这一个文件，不整目录放行。
python/Lib/site-packages/fontTools/cu2qu/ufo.py
